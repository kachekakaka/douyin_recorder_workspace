# 2026-07-19 授权现场预检：73504089679

## 1. 记录范围

用户明确提供并授权用于 P0 现场测试的抖音号/直播路径标识：

```text
73504089679
```

本次只访问公开直播页并使用无登录 Chrome/CDP 被动观察网页创建的 WebSocket。没有登录、没有绕过验证码、没有发送互动消息，也没有保存或上传 Cookie、完整签名 URL、网页正文、原始帧、真实 payload 或 recipient 明文 ID。

## 2. 对应 GitHub 记录

```text
分支提交：39520e699542d5394dde846f2e52d845d5924ba9
CI run：29669746416（success）
P0 Douyin live preflight run：29669746415（success）
观察窗口：2026-07-19 02:09:31–02:10:40 UTC
```

CI 的 Python 3.12、Python 3.13、Ruff、pytest、确定性 replay、前端语法、Windows `verify.bat` 和 Git Bundle/源码 ZIP 恢复验证全部通过。

## 3. HTTP 直播页预检

结果：

- HTTP 200；
- 无重定向；
- 最终地址为 `live.douyin.com/73504089679`；
- 返回约 1.25 MB HTML；
- 页面源码中出现 `live_core_sdk_data`、`stream_data`、`flv_pull_url`、`hls_pull_url`、`roomId`、`room_id`、`web_rid` 和 `webcast` 等标记；
- 当前安全解析器没有从该响应提取出可靠的 room ID、web_rid、状态值或脱敏 WSS endpoint；
- 源码包含字符串 `captcha`，但仅凭字符串存在不能证明浏览器实际展示了交互验证码。

解释：公开直播页可访问，并包含明显的直播/流元数据结构，但这些线索不能替代真实 IM WebSocket 和目标消息验证。

## 4. Chrome/CDP 被动 WSS 观察

60 秒观察结果：

```text
page_loaded=true
websocket_count=0
binary_frame_count=0
envelope_frame_count=0
message_count=0
target_message_count=0
target_decode_success_count=0
target_decode_failure_count=0
transport_live_verified=false
target_live_verified=false
```

Chrome 正常启动，页面 load event 已触发，探测器自身没有报错；但本次无登录 headless 页面没有创建可观察的抖音 WSS，因此没有机会验证 outer envelope、gzip、ACK、heartbeat 或 `WebcastGroupLiveGiftRecipientRecommendMessage`。

可能原因包括：测试时房间未处于有效直播状态、无登录/自动化环境触发页面降级、页面需要额外交互或游客上下文、观察窗口内没有建立 WSS。当前证据不足以区分这些原因。

## 5. 结论

本次授权测试证明：

1. 该标识对应的公开直播页能够返回包含直播流相关结构的页面；
2. GitHub Actions 中的 HTTP 与浏览器预检工具可安全运行并只产出脱敏报告；
3. 本次没有观察到 IM transport，也没有观察到目标推荐收礼人消息。

因此运行 contract 必须继续保持：

```text
live_verified=false
```

不得把“未观察到”解释为“该房间永久不支持目标消息”，也不得据此进入依赖现场协议事实的完整 P1 自动录制闭环。

## 6. 下一次现场验证建议

- 在已人工确认该账号正在团播、且能够实际切换推荐收礼人的时间段运行；
- 优先使用普通交互浏览器捕获当前完整 WSS URL，再按 `docs/protocol/CAPTURE_RUNBOOK.md` 在私人 `userdata/` 中运行显式 WSS probe；
- 观察至少 2–5 分钟，并在观察期内确认发生至少一次成员切换；
- 真实 raw frame 和 payload 只保存在私人目录；提交仓库前只制作去标识、可回放、人工审查过的最小 fixture；
- 只有目标 method、字段位置、空值、重复、重连与时间延迟均有证据后，才修改 `live_verified`。
