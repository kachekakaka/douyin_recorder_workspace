# V0.1.1 Next Steps

## Phase 1 — completed

Runtime diagnostics, SQLite integrity validation, and backup/restore verification were merged by
PR #22.

## Phase 2 — completed

Safe database maintenance and repeated startup recovery were merged by PR #24. Issue #23 is
closed as completed.

Delivered:

- read-only maintenance planning;
- verified backup before explicit checkpoint/optimize;
- database-lock and migration-checksum fault injection;
- repeated recording/postprocess startup recovery;
- Linux, Windows and portable-package verification.

## Phase 3 — implemented and validated

Issue #25 and PR #26 contain the operator-experience scope:

- source and portable `operations.bat` entrypoints;
- redacted diagnostics command;
- read-only maintenance-plan command;
- explicitly confirmed maintenance-apply command;
- portable clean-extraction execution in a Unicode path;
- README, implementation report and v0.1.1 Release notes draft.

The implementation head `3158a5f00418dde40b7b0f07e17b9f1b3c5e689a` passed standard CI
`30018893927` and Windows/source package validation `30018890863`. GitHub PR state is the
canonical source for final merge status.

## Next audited batch

After PR #26 is merged, review the final `main` tree and create a dedicated v0.1.1
release-preparation Issue if publication is desired. That batch must independently handle:

- synchronized version changes;
- release-lock and asset-name changes;
- exact-main CI and Windows package validation;
- annotated tag creation;
- immutable GitHub Release and asset audit.

Version changes, tag creation and Release publication must not occur as a side effect of Phase 3.
Protocol evidence remains outside v0.1.1 and continues through Issue #1. The contract remains
`live_verified=false`.
